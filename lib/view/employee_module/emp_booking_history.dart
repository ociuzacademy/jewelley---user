import 'package:flutter/material.dart';

class EmpBookingHistory extends StatelessWidget {
  const EmpBookingHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}